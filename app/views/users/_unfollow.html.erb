<%= form_for(current_user.active_connections.find_by(followed_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unfollow", class: "button is-danger connection" %>
<% end %>